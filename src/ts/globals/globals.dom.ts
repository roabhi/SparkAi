export const myOutputNode = document.getElementById(
    'dialog-box'
  ) as HTMLElement,
  myForm = document.querySelector('form') as HTMLElement,
  myInputField = myForm.querySelector('input') as HTMLInputElement,
  mySendButton = myForm.querySelector('button') as HTMLButtonElement
